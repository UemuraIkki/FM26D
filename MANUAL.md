# FM26D 操作説明書

「実際にどう動かすか」だけをまとめた実用ガイド。技術的な設計・実装内容は [README.md](README.md) を参照。

## このシミュレーターの性質

FM26D は GUI のないヘッドレス CLI シミュレーターで、いわゆる「放置観測ゲーム」です。

- 自分でクラブを操作するモード(監督采配・移籍交渉を自分で行う)は**まだありません**。全クラブが AI(`AIDecisionMaker`)によって自律的に運営されます。あなたの役割は「世界を作って時間を進め、結果を眺める・気になる選手を追いかける」ことです。
- 試合は内部的に1プレー単位でシミュレートされますが、CLI では結果のサマリ(順位表・移籍・ニュース)のみが表示されます。実況のような画面はありません。
- 同じ `--seed` を指定すれば、何度実行しても**完全に同じ歴史**が再現されます(決定論)。

## 事前準備

```bash
npm install
```

Node.js 22.5 以上が必要です(`node:sqlite` を使用するため)。

## 基本の流れ

1. **`season`** — まず1シーズンだけ試しに実行して雰囲気を掴む(セーブされない使い捨て実行)
2. **`play`** — 本命の遊び方。複数シーズンを SQLite ファイルに保存しながら連続実行していく
3. **`feed` / `watch` / `watchlist`** — 保存したセーブから、ニュースを追ったり気になる選手を追跡したりする
4. **`depth`** — 特定クラブの選手層(誰が主力で誰が余剰か)を覗く
5. **`calibrate` / `healthcheck`** — シミュレーターそのものの健全性を検証する(開発・デバッグ用)

すべて `npm run <コマンド> -- [オプション]` の形式で実行します(`--` の後にオプションを書きます)。

## `--leagues` オプション(複数コマンド共通)

どのリーグをシミュレートするかを選びます。

| 指定できる名前 | リーグ |
|---|---|
| `premier-league` | プレミアリーグ |
| `la-liga` | ラ・リーガ |
| `bundesliga` | ブンデスリーガ |
| `serie-a` | セリエA |
| `ligue-1` | リーグ・アン |

- 省略時は `premier-league` のみ(単一リーグ、動作が速い)
- カンマ区切りで複数指定可: `--leagues premier-league,la-liga`
- `all`(または `big5`)で5大リーグ全部: `--leagues all`

**2リーグ以上を選ぶと、UEFAチャンピオンズリーグとリーグ間移籍、代表戦(国際親善試合ウィンドウ + 4年に1回のW杯/EURO)が自動的に有効になります**。個別のON/OFFフラグはなく、リーグ数だけで自動判定されます。

5大リーグ全部(`all`)は内部で数十万試合をシミュレートするため、実行に数分かかります。まず試すだけならデフォルト(プレミアリーグ単体)が数秒で終わって手軽です。

## コマンド一覧

### `npm run season -- [オプション]`

1シーズンだけ実行して結果をターミナルに表示し、終了します。セーブはされません(お試し・検証向け)。

| オプション | 説明 | 既定値 |
|---|---|---|
| `--seed N` | 乱数シード。同じ値なら同じ結果 | `20260808` |
| `--year N` | シーズン開始年 | `2026` |
| `--leagues LIST\|all` | シミュレートするリーグ | `premier-league` |

表示内容: 各リーグの最終順位表 → (2リーグ以上なら)チャンピオンズリーグ/W杯/EUROの優勝 → 試合統計(平均得点・パス成功率など) → 移籍一覧 → 移籍拒否一覧 → 監督交代一覧 → 契約更新/解約件数 → 資金保存則ドリフト(必ず `0` — それ以外なら不具合)。

```bash
npm run season -- --seed 42
npm run season -- --leagues all          # 5大リーグ+CL+W杯/EUROを1シーズンだけ試す
```

### `npm run play -- --seasons N --db <path> [オプション]`

**本命のプレイ方法。** ワールドを SQLite ファイルに保存しながら、指定シーズン数を連続実行します。

| オプション | 説明 |
|---|---|
| `--seed N` | 新規ワールド作成時の乱数シード(既定 `20260808`) |
| `--seasons N` | 今回実行するシーズン数(既定 `1`) |
| `--until YEAR` | このシーズン開始年に到達したら停止(`--seasons` と併用可)。**シーズン単位**の制御で、季の途中では止まりません |
| `--db <path>` | セーブファイルのパス(既定 `fm26d.sqlite`) |
| `--resume` | 既存セーブの続きから実行 |
| `--leagues LIST\|all` | 新規ワールド作成時のみ有効。`--resume` 時はセーブ済みのリーグ構成がそのまま使われ、この指定は無視されます |

```bash
npm run play -- --seed 1 --seasons 5 --db world.sqlite --leagues all
npm run play -- --seasons 3 --db world.sqlite --resume       # 続きから3シーズン進める
```

実行するたびに1シーズンごとの要約行(各リーグの優勝クラブ、CL/W杯/EUROの優勝、発生ニュース件数)が表示されます。

### `npm run feed -- --db <path> [オプション]`

保存したワールドのニュースフィードを検索します。

| オプション | 説明 |
|---|---|
| `--type TYPE` | `TRANSFER` / `TRANSFER_REFUSED` / `MANAGER_SACKED` / `MANAGER_HIRED` / `RETIREMENT` / `TITLE` / `RECORD_100_APPS`(通算100試合出場) / `FIRST_CAP`(代表初キャップ) |
| `--player ID` | 特定選手のニュースだけに絞る |
| `--limit N` | 表示件数上限(既定 `50`) |

```bash
npm run feed -- --db world.sqlite --type TITLE
npm run feed -- --db world.sqlite --player LIV-01
```

### `npm run watch -- --db <path> --player <ID>` / `npm run unwatch -- ...`

選手をウォッチリストに登録/解除します。選手IDは `クラブID-連番`(例: `LIV-01`)の形式で、`feed` の出力や後述の `watchlist` に登場します。squad一覧そのものを見るCLIコマンドは今のところないため、まずは `feed` に出てきた選手や、応援したいクラブの主力(`depth --club` で確認可能)のIDを控えておくと良いです。

### `npm run watchlist -- --db <path>`

ウォッチ中の選手の現況一覧。所属クラブ、年齢、能力値、通算出場数、代表キャップ数を表示します。引退済みの選手は `RETIREMENT` と表示され、アーカイブ経由で追跡が続きます(主要選手=通算100試合以上または代表歴ありは永久保存、それ以外は引退5年後に詳細情報が削除されます)。

### `npm run depth -- [オプション]`

デプスチャート(選手層)の確認。

| オプション | 説明 |
|---|---|
| `--club ID` | 指定クラブの詳細(ロールごとの1st/2nd/3rd + 不足/余剰)を表示。省略時は全クラブの不足/余剰サマリのみ |
| `--seed N` | 乱数シード |
| `--leagues LIST\|all` | 対象リーグ |

```bash
npm run depth -- --club ARS
npm run depth -- --leagues la-liga --club RMA
```

### `npm run calibrate -- --seasons N [--seed N] [--leagues LIST]`

複数シーズンにわたる統計的キャリブレーション(1試合平均得点・パス成功率・ホームアドバンテージなど)を検証します。開発・デバッグ向けで、通常のプレイでは使いません。

### `npm run healthcheck -- [--seasons 10] [--seed N] [--out path.csv] [--leagues LIST]`

長期(複数シーズン)健全性テスト。リーグ平均年齢/能力のドリフト、優勝クラブの偏り(ジニ係数)、引退対新人供給バランス、資金保存則を自動検証し、CSVに書き出します。こちらも開発・デバッグ向けです。

## 出力の読み方

- **順位表**: `P`=試合数, `W`/`D`/`L`=勝分敗, `GF`/`GA`=得点/失点, `GD`=得失点差, `Pts`=勝点
- **移籍拒否理由**: `PLAYING_TIME`(出場機会不足)/ `REPUTATION`(クラブの格が不満)/ `WAGE`(待遇不満)/ `OVERALL`(総合判断)
- **監督交代理由**: `MID_SEASON_SACKING`(シーズン中の解任)/ `END_OF_SEASON`(シーズン終了時の解任)
- **資金保存則ドリフト**: 必ず `0`。0以外が出た場合はバグの兆候です

## Tips

- **決定論**: 同じ `--seed` なら何度実行しても同じ結果になります。違う展開を見たいときは `--seed` を変えてください
- `data/rosters/*.json` が用意されている5大リーグは実在の選手名(氏名・ポジション・年齢・国籍)で開始します。能力値(17属性)は実データではなく自作procedural生成です(FM/FIFA等の商用データベースを模倣・転記していないため)
- SQLite利用時に出る `ExperimentalWarning: SQLite is an experimental feature` は Node.js 組み込み機能の警告で、動作には影響ありません。無視して構いません

## 現状できないこと

- 自分の手でクラブを操作するモード(監督采配・移籍交渉を自分で行う)。今は全クラブAI運営の観測専用です
- 試合の実況・プレー単位の閲覧(内部ではイベントログを持っていますが、CLIには結果サマリしか表示されません)
- 日単位の一時停止・再開(`play` はシーズン単位でのみセーブ/再開できます)
- 実際のカレンダーに沿った日程(シーズンは常に架空の固定日程で進みます)
